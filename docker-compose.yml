# transfer deployment stack
#
# Usage:
#   cp .env.example .env                            # then edit .env
#   mkdir -p auth                                   # auth directory
#   htpasswd -B -c auth/htpasswd <user>             # create the auth file
#   sudo chown -R 65532:65532 auth                  # match container UID
#   docker compose up -d
#
# Reverse-proxy (TLS termination) is expected to live OUTSIDE this compose file
# and forward to the `transfer` service over the shared `proxy` network.
#
# By default the stack joins an EXTERNAL bridge network called `proxy`.
# Override with the PROXY_NETWORK variable in .env if your reverse-proxy lives
# on a network with a different name.
#
# Migrating from <= v1.5.3 (single-file htpasswd bind):
#   mkdir -p auth && mv htpasswd auth/htpasswd && sudo chown -R 65532:65532 auth
#   docker compose up -d

name: transfer

services:
  transfer:
    image: ${TRANSFER_IMAGE:-ghcr.io/xenofex7/transfer:latest}
    restart: unless-stopped
    environment:
      LISTENER: ":8080"
      BASEDIR: "/data"
      TEMP_PATH: "/tmp/transfer"
      PURGE_DAYS: "${PURGE_DAYS:-360}"
      PURGE_INTERVAL: "${PURGE_INTERVAL:-1}"
      MAX_UPLOAD_SIZE: "${MAX_UPLOAD_SIZE:-10485760}"  # in KB, default 10 GiB (signed-in tier)
      ANON_UPLOADS: "${ANON_UPLOADS:-true}"
      ANON_MAX_UPLOAD_SIZE: "${ANON_MAX_UPLOAD_SIZE:-51200}"  # in KB, default 50 MiB
      ANON_UPLOAD_TTL: "${ANON_UPLOAD_TTL:-24h}"
      AUTH_UPLOAD_TTL: "${AUTH_UPLOAD_TTL:-4320h}"     # 180 days
      RATE_LIMIT: "${RATE_LIMIT:-0}"
      RANDOM_TOKEN_LENGTH: "${RANDOM_TOKEN_LENGTH:-10}"
      HTTP_AUTH_HTPASSWD: "/etc/transfer/htpasswd"
      HTTP_AUTH_IP_WHITELIST: "${HTTP_AUTH_IP_WHITELIST:-}"
      IP_WHITELIST: "${IP_WHITELIST:-}"
      IP_BLACKLIST: "${IP_BLACKLIST:-}"
      CORS_DOMAINS: "${CORS_DOMAINS:-}"
      PROXY_PATH: "${PROXY_PATH:-}"
      PROXY_PORT: "${PROXY_PORT:-}"
      EMAIL_CONTACT: "${EMAIL_CONTACT:-}"
      TAGLINE: "${TAGLINE:-Easy and fast file sharing from the command line.}"
      UPLOAD_WEBHOOK_URL: "${UPLOAD_WEBHOOK_URL:-}"
      WEBHOOK_TOKEN: "${WEBHOOK_TOKEN:-}"
      UMAMI_SCRIPT_URL: "${UMAMI_SCRIPT_URL:-}"
      UMAMI_WEBSITE_ID: "${UMAMI_WEBSITE_ID:-}"
      UMAMI_HEARTBEAT: "${UMAMI_HEARTBEAT:-}"
      UMAMI_HEARTBEAT_URL: "${UMAMI_HEARTBEAT_URL:-}"
      UMAMI_HEARTBEAT_WEBSITE_ID: "${UMAMI_HEARTBEAT_WEBSITE_ID:-}"
    volumes:
      - ${DATA_DIR:-./data}:/data
      # Mounted read-write so the /admin/users UI can add/reset/delete
      # users in place. The directory (not just the file) is mounted so
      # the userStore can atomically rename .htpasswd-*.tmp into place.
      # Host dir must be owned by UID 65532 (the container user); keep
      # it outside any world-readable path on the host.
      - type: bind
        source: ${AUTH_DIR:-./auth}
        target: /etc/transfer
        bind:
          create_host_path: false
    networks:
      - proxy
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:8080/health.html"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    mem_limit: ${TRANSFER_MEMORY_LIMIT:-512M}
    security_opt:
      - no-new-privileges:true

networks:
  proxy:
    name: ${PROXY_NETWORK:-proxy}
    external: true
